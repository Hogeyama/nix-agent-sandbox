.coverage/
.hostexec-demo/
src/ui/dist/
.playwright/cli.config.json
.playwright-cli/
# devShell が張る tree-sitter grammar への symlink（store path を含む）
.ast-grep/
.worktrees/
.direnv/
node_modules/
docs-site/.astro/
docs-site/dist/
result
zig-out/
.zig-cache/
__pycache__/
*.pyc
# Populated by `bun run vendor` (uv pip install --target, see
# src/docker/mitmproxy/vendor-requirements.txt); the nix build fetches the
# same sdist instead.
src/docker/mitmproxy/vendor/
