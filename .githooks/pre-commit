#!/bin/sh
set -e

if ! command -v deno >/dev/null 2>&1; then
  echo "Warning: deno not found, skipping pre-commit checks"
  exit 0
fi

echo "Running deno fmt..."
deno fmt --check

echo "Running deno lint..."
deno lint

echo "Running deno test..."
deno test --allow-all
