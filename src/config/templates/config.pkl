amends "modulepath:/global.pkl"

// To amend the schema directly instead of inheriting from global.pkl,
// replace the line above with:
//   amends "Schema.pkl"

// Give a secret a name, then say where it may be hidden or sent:
//   ["default"] {
//     secrets {
//       ["db-password"] { from = "dotenv:.env#DB_PASSWORD" }
//     }
//     // Hide it from the agent (any file-read path shows same-length "*"):
//     mask = new MaskConfig {}
//     // Reach one host, and send the secret only there:
//     network {
//       scopes {
//         ["api"] {
//           targets { "api.example.com:443" }
//           fallback = "allow"
//           secrets { ["db-password"] = "inject" }
//           inject {
//             new Inject {
//               name = "Authorization"
//               value = #"template:Bearer ${db-password}"#
//             }
//           }
//         }
//       }
//     }
//   }

profiles {
  ["default"] {
    agent = "claude"
  }
}
