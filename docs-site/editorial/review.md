# 文書改訂の確認記録

追記：以下は削除前のレビュー記録。`getting-started/about.md` は、想定読者への導線とページの役割が整合していなかったため、ユーザーの指示により削除した。サイドバーと本文の参照も削除済み。初回レビューではこの不整合を検出できていなかった。

2026-09-05。[ページの編集設計](pages.md)に基づき、独立したレビュアーが公開本文30ページを評価した。初回に19ページが合格、11ページに6群の不足が見つかった。該当箇所を修正し、11ページを再確認して合格した。技術的な検証は後述のとおり別に実施した。

## 初回で目的を満たしたページ

| ページ | 判定 | 本文から得られる答え |
| --- | --- | --- |
| index.mdx | 合格 | `.env` を隠して編集を任せる、必要な通信だけ通す、認証付きコマンドを承認する用途を自分の作業に当てはめ、Linux/Docker と設定の手間を判断できる。「用途」の「ソースコードの編集は任せつつ」と「起動までの作業」の「設定の追加や承認が必要」が効用と継続負担の根拠。用途ラベルには機能的な名詞もあるが、本文は作業の変化になっている。 |
| getting-started/about.md | 合格 | ホストの定義から、既定で書き換えられる作業フォルダー・認証設定と追加権限を区別できる。「既定の共有範囲」の「変更はホストにも残る」と「共有範囲の追加」の読み取り専用共有／ホスト実行の比較が判断材料。 |
| getting-started/quick-start.md | 合格 | Claude 用と明示したうえで `.nas/config.pkl` 全文、信頼、起動、API 応答確認まで進める。「設定の確認と起動」の「応答を確認できれば」が成功条件で、失敗時の監査ログへの導線もある。 |
| getting-started/configuration.md | 合格 | プロファイル名と起動コマンドを対応させ、機能設定の追加位置、トップレベルの例外、継承、再信頼を選べる。「プロファイルの編集」の「ui と observability だけはトップレベル」と全体例が配置を具体化している。 |
| features/filesystem.md | 合格 | キャッシュの追加共有と `.env` の非公開を別々に設定でき、相対パス・ro/rw・変更の残存を判断できる。「ファイルの非公開」の /dev/null 例と「設定項目」のパス基準が根拠。 |
| features/port-forwarding.md | 合格 | ホストの DB/API をコンテナの同番号 localhost から使う設定と接続先が分かる。「設定例」の 5432/8080 対応と「注意点」の「サービス側で認証」が、方向と追加されない保護を示す。 |
| features/hostexec.md | 合格 | ホスト権限を先に知り、uptime の最小ルール→ホストでの test→再信頼→要求→承認→結果確認を実施できる。「ホストの稼働時間がコマンドの結果として返ります」が到達確認。秘密注入の詳細は目的を明記したレシピへ送っている。 |
| features/secrets.md | 合格 | dotenv 取得元と mask を組み合わせてファイル・出力を隠し、登録だけでは注入しないと判断できる。「同じ長さの *」「名前を登録しただけでは…入りません」と設定例が根拠。注入先ごとの指定は担当ページへ委ねている。 |
| features/nix.md | 合格 | `/nix` による自動有効化、devShell と追加パッケージ、無効化、共有の影響を判断できる。「設定例」の gh/jq と「注意点」の「ホストの Nix の状態にも影響」が根拠。 |
| features/docker.md | 合格 | テスト用 daemon の有効化、イメージ取得時の通信許可、データ寿命と補助コンテナの privileged を判断できる。「データとキャッシュの保持期間」が削除／再利用を具体化し、403 時はネットワーク設定へ誘導する。通信許可の説明は同節の末尾で見つけにくいが、作業に必要な案内はある。 |
| features/worktree.md | 合格 | 一回限りの `nas -b main claude` と毎回の設定を選び、終了時の未コミット変更・固有コミットをどう扱うか判断できる。「注意点」の stash、cherry-pick 失敗時の残存、使用中でも削除される説明が根拠。 |
| features/sessions.md | 合格 | dtach の前提、切り離しキー、ID 取得と再接続、使用エージェントの通知 hook 設定を辿れる。「nas session list」「nas session attach」と「使用するエージェントの例を選んで」が根拠。エージェント別見出しで不要な例を飛ばせる。 |
| features/display.md | 合格 | GUI 用のホスト・コンテナ前提、画面サイズ、専用画面と入力の共有、終了時処理を判断できる。「設定例」と「xpra ビューアーへのキー入力と貼り付け」が根拠。実際の起動・エラー対処は明示したレシピに分かれている。 |
| features/ui.md | 合格 | ホストの `nas ui` から画面ごとの操作に進み、トップレベル設定と同一ホスト利用者への公開範囲を判断できる。「画面の操作」の表と「そのユーザーも画面とトークンを取得できます」が根拠。 |
| features/observability.md | 合格 | 常時の履歴と追加の本文・利用量収集を分け、有効化と保持期間を設定できる。「プロンプトやツールの入出力本文も保存され得ます」、保存先 history.db、retention の削除時機が判断材料。 |
| operations/approvals.md | 合格 | pending の一覧から ID を取り、一回限りの承認・拒否と再利用範囲を選べる。「省略時は常に once」と HostExec の既定 capability の差、パスを再利用条件に含まない説明が根拠。 |
| security/model.md | 合格 | ホストに変更を及ぼすファイル・コマンド・通信経路、設定信頼が保証しない参照ファイル、承認ソケットの分離、セッション間共有を説明できる。「ホストへの経路が専用ソケットだけとは限りません」が単純化しすぎない境界の証拠。 |
| security/risks.md | 合格 | 既定、有効化、到達先とリスクを同じ表で比較できる。Nix の自動有効化、DBus の取得対象の非限定、UI の同一ホストユーザーに対する限界が具体的な判断材料。手順全文を表に詰めていない。 |
| security/recommendations.md | 合格 | ファイル・認証、ホスト実行、通信・画面、保存データの順で自分の設定を点検できる。「注入だけでは出力を隠せません」「転送機能はサービスのアクセス制御を追加しません」が具体的な点検基準。 |

## 修正後の確認

| ページ | 再判定 | 修正後に読み取れる到達点・証拠 |
| --- | --- | --- |
| getting-started/installation.md | 合格 | 前提条件の「nas を実行するユーザーで docker info が成功」で実行権限を確認し、「導入の確認」の nas --version で導入を確認できる。任意の展開先も ~/.local/lib/nas となり、初回導入とは分離されている。 |
| operations/maintenance.md | 合格 | イメージ削除、worktree の未コミット変更の削除、補助コンテナ・一時ボリュームの削除範囲が各実行ブロックの前にある。worktree の「必要な変更を保存してから実行」と使用中確認により、失うものを判断してから実行できる。 |
| security/limitations.md | 合格 | 「異常終了後のデータ」に mask-filter/<session-id>/mask-secrets の実パス、XDG_RUNTIME_DIR 未設定時の代替、id -u があり、停止確認後に削除すべきセッションディレクトリを特定できる。 |
| operations/audit.md | 合格 | 「保存先」で既定の audit.db と --audit-dir 指定時の場所を特定できる。本文の自動削除と判定記録の手動管理の区別も残り、対象を取り違えず保持を管理できる。 |
| recipes/mask-env.md | 合格 | 通信設定済みの claude に断片を追加し、「通信先やルールを残してください」と明示。test と nas claude も一致し、既存の通信を保持したままビルド用の非公開・注入・マスクを設定できる。 |
| recipes/relative-hostexec.md | 合格 | 通信設定済みの claude への追加となり、test と起動が対応。作業ディレクトリと変更確認、一回限りの承認まで途切れず進める。 |
| recipes/proxy-tools.md | 合格 | 「前提条件」で通信可能な状態を要求し、既存プロファイルの通信先を残して Gradle 配布元を追加する。JVM プロパティと許可の区別、必要な追加配布元の案内があり、以前の API 許可消失は解消している。 |
| recipes/codex-keyring.md | 合格 | 通信設定済みの codex に dbus 部分のみ追加し、nas codex で起動する。保存済み認証・Secret Service の前提と、他の秘密値も取得できる権限範囲を保持している。 |
| recipes/x11-apps.md | 合格 | 通信設定済みの claude に display だけを追加し、nas claude 起動後に GUI アプリを要求できる。ホスト・コンテナの前提とエラー別の確認は残っている。 |
| features/network.md | 合格 | 「秘密値とヘッダー」に env:API_TOKEN の登録、既存 example-api の inject 許可、Authorization の Bearer テンプレートの入力形式が揃う。「既存の GET・パス制限を残したまま」とホスト側の値の設定、再信頼、成功確認まであり、認証付き通信を設定できる。 |
| features/port-bind.md | 合格 | 「CLI 操作」がホストでの Docker 一覧から始まり、コンテナ名とセッション ID を取得して bind へ渡す。成功時の URL と localhost:8080 の例により、公開後に開く先を確定できる。 |

## 技術検証

- `bun run docs:build`：成功。404 を含む31ページを生成し、内部リンク・アンカー・画像と検索索引の検査を通過。
- Pkl の全22例：成功。リポジトリの `Schema.pkl` と `global.pkl` で評価。断片は本文が指定するトップレベルまたはプロファイルに配置し、ヘッダー注入の追加例は既存の通信ルールを持つプロファイルへの追加として評価した。
- `git diff --check`：成功。
- ページタイトル・見出し・ナビゲーション：名詞句であることを確認。
- 新しいスキル：frontmatter と参照先を確認。標準検査スクリプトは PyYAML がなく実行できなかったため、Bun の YAML パーサーで同じ frontmatter 制約を確認した。

文書中の導入・削除・認証・ホスト実行コマンドを実環境に対して実行したわけではない。ビルドや Pkl の成功は、それらの実行結果まで保証しない。

## 執筆手順の確認

[評価ケースと結果](../../skills/reader-decision-writing/references/evaluation.md)を参照。独立した書き手による架空のバックアップ製品への適用では、利用判断に必要な情報の選択と、削除操作前の影響説明を確認した。
